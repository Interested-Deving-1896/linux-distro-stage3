name: Mirror Interested-Deving-1896 → OSP

on:
  schedule:
    - cron: '0 */6 * * *'
  workflow_dispatch:
    inputs:
      repo_filter:
        description: "Mirror only this repo name (leave blank for all)"
        required: false
        default: ""
        type: string
      dry_run:
        description: "Report without pushing to OSP"
        required: false
        default: false
        type: boolean
      force:
        description: "Force-push even if OSP branch has diverged"
        required: false
        default: false
        type: boolean

permissions:
  contents: write  # Changed from read to write to allow pushing to OSP

concurrency:
  group: mirror-to-osp-${{ github.ref }}
  cancel-in-progress: true

jobs:
  mirror:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Mirror matched repos from upstream into OSP
        env:
          GH_TOKEN: ${{ secrets.SYNC_TOKEN }}
          UPSTREAM_OWNER: Interested-Deving-1896
          OSP_ORG: OpenOS-Project-OSP
          BUDGET_MINUTES: "25"
        run: bash scripts/mirror-to-osp.sh
